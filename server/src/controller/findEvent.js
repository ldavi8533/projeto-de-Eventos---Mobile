const Event = require('../database/evento')

const objects = [
    Event.map()
];

array.forEach(obj => {
        Event.filter(nome)
});

function findEvent() {
    return objects.filter(Event.nome);
};




module.exports = findEvent;